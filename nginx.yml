services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"

    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    # depends_on:
    # - jira  
    # - jira-node2
    # networks:
    #   - host
# networks:
#   macvlan_network:
#     driver: macvlan
#     driver_opts:
#       parent: eth0
#     ipam:
#       config:
#         - subnet: 192.168.1.0/24
#           gateway: 192.168.1.1
# networks:
#   host:
#     external: true